<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:id="@+id/main_category_layout"
    android:orientation="vertical">

    <!--<TextView
        android:id="@+id/history_title"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:layout_gravity="center"
        android:background="@color/main_bg_gray"
        android:padding="@dimen/resolution_padding"
        android:text="@string/history_title"
        android:gravity="center_vertical"
        android:visibility="gone"
        android:textAppearance="@style/textStyle_heading" />

    <TextView
        android:id="@+id/nohistory_tv"
        android:layout_width="match_parent"
        android:paddingLeft="@dimen/history_without_test"
        android:paddingRight="@dimen/history_without_test"
        android:paddingTop="@dimen/category_text_padding"
        android:layout_height="match_parent"
        android:gravity="center"
        android:textSize="@dimen/history_without_test"
        android:visibility="gone"/>-->
    <LinearLayout
        android:id="@+id/pick_category_text"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:visibility="gone"
        android:minHeight="?attr/actionBarSize"
        android:orientation="vertical"
        android:padding="11dp">

        <TextView
            android:id="@+id/text_pick_category"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:gravity="left"
            android:singleLine="true"
            android:text="@string/pick_a_test"
            android:textAppearance="@style/textStyle_heading" />
    </LinearLayout>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >

        <ListView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:divider="@android:color/transparent"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_marginBottom="10dp"
            android:dividerHeight="10.0sp"
            android:id="@+id/listview"/>
        <!-- <ProgressBar
             android:id="@+id/historyLoadingProgress"
             android:layout_width="90dp"
             android:layout_height="90dp"
             android:padding="10dp"
             android:indeterminate="true"
             android:layout_centerInParent="true"
             android:max="100"
             android:progress="100"
             android:visibility="gone" />-->
    </RelativeLayout>
</LinearLayout>


